openapi: 3.0.3
info:
  title: Acuity Portal API
  version: 2026-07-29
  description: >
    Acuity Portal Access, HumanCalling, Work, and Messaging interfaces with
    PostgreSQL authority, durable commands and receipts, and disposable
    realtime hints.
servers:
  - url: https://api.acuity.example
    description: Deployment-supplied HTTPS origin
paths:
  /health/live:
    get:
      operationId: getLiveness
      security: []
      responses:
        "200":
          description: Process is running.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
  /health/ready:
    get:
      operationId: getReadiness
      security: []
      responses:
        "200":
          description: Required runtime dependencies are ready.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access/sign-up-eligibility:
    post:
      operationId: inspectSignUpEligibility
      summary: Validate an Access Grant or provisioned Platform Operator email.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignUpEligibilityRequest"
      responses:
        "200":
          description: The verified-email sign-up is eligible.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SignUpEligibility"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/access:
    get:
      operationId: discoverAccess
      summary: Discover current Practice and Location authorization.
      responses:
        "200":
          description: Current Access authority from PostgreSQL.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessDiscovery"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/workspace:
    get:
      operationId: getWorkspace
      summary: Load the authoritative empty workspace snapshot.
      parameters:
        - name: practiceId
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: locationId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Authorized workspace snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceSnapshot"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/practices/{practiceId}/locations:
    post:
      operationId: addLocation
      summary: Add a Location as a Platform Operator.
      parameters:
        - name: practiceId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddLocationRequest"
      responses:
        "201":
          description: Committed Location mutation and audit.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LocationMutation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/events:
    get:
      operationId: getEvents
      summary: Stream disposable version hints for one authorized scope.
      parameters:
        - name: practiceId
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: locationId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: >
            Bounded text/event-stream. Initial connection and every reconnect
            require an authoritative workspace refetch.
          content:
            text/event-stream:
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/handoffs:
    post:
      operationId: createHandoff
      summary: Create one authenticated, idempotent Abita handoff.
      security:
        - serviceBearer: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateHandoffRequest"
      responses:
        "201":
          description: Opaque single-use SIP handoff.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Handoff"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/provider/telnyx/webhooks:
    post:
      operationId: receiveTelnyxWebhook
      summary: Verify and durably receipt one raw Telnyx webhook.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "204":
          description: Receipt durably committed or accepted duplicate.
        "400":
          $ref: "#/components/responses/BadRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/softphone/lease:
    post:
      operationId: acquireSoftphone
      summary: Acquire, renew, or explicitly take over the User's softphone lease.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoftphoneLeaseRequest"
      responses:
        "200":
          description: Current softphone ownership.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoftphoneState"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/readiness:
    put:
      operationId: setCallingReadiness
      summary: Commit the media-owning session's current technical readiness.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CallingReadinessRequest"
      responses:
        "200":
          description: Current derived operational state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoftphoneState"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/media-token:
    post:
      operationId: issueCallingMediaToken
      summary: Issue a short-lived Telnyx JWT to the current softphone owner.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MediaTokenRequest"
      responses:
        "200":
          description: Short-lived browser media credential.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaToken"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/state:
    get:
      operationId: getCallingState
      summary: Read durable softphone and CallLeg convergence state.
      responses:
        "200":
          description: Current authorized calling state.
          headers:
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallingState"
        "304":
          description: Calling state has not changed.
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/outbound-calls:
    post:
      operationId: startOutboundCall
      summary: Commit one Task-originated or standalone outbound Call before provider contact.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartOutboundCallRequest"
      responses:
        "201":
          description: Durable outbound Call attempt.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallingCall"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/tasks/{taskId}/eligibility:
    get:
      operationId: getTaskOutboundEligibility
      summary: Explain whether the current Task route is eligible for outbound calling.
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Current server-derived Task call eligibility.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutboundCallEligibility"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/calls/{callId}:
    get:
      operationId: getCallingCall
      summary: Read the winning User's durable active or disposition Call.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Authorized Call workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallingCall"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/calls/{callId}/media-ready:
    post:
      operationId: confirmCallingMediaReady
      summary: Confirm that the owning browser attached the answered staff media session.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfirmCallingMediaRequest"
      responses:
        "200":
          description: The outbound Call after destination dialing was safely committed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallingCall"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/calls/{callId}/retry:
    post:
      operationId: retryOutboundCall
      summary: Commit an intentional outbound retry linked to a terminal or unknown attempt.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RetryOutboundCallRequest"
      responses:
        "201":
          description: New linked outbound Call attempt.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallingCall"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/calls/{callId}/hangup:
    post:
      operationId: requestCallingHangup
      summary: Commit a provider hangup command without claiming termination.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CallingControlRequest"
      responses:
        "202":
          description: Hangup intent committed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallingCall"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/calls/{callId}/disposition:
    post:
      operationId: recordCallingDisposition
      summary: Resolve the provider-terminated Call and optionally ensure its one follow-up Task.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CallingDispositionRequest"
      responses:
        "200":
          description: Durable terminal Call outcome and optional linked Task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallingDispositionResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/calls/{callId}/history:
    get:
      operationId: getCallingCallHistory
      summary: Read authorized exact-phone Call Engagement History for the active Call.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Newest page presented in chronological order.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallHistoryPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/calls/{callId}/engagement-history:
    get:
      operationId: getCallingEngagementHistory
      summary: Compose exact-phone Messages, Calls, and Tasks across currently authorized Practice Locations.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
      responses:
        "200":
          description: Chronological Contact Context Engagement History.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationTimelinePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/calls/{callId}/voicemail-playback:
    post:
      operationId: issueCallingVoicemailPlayback
      summary: Issue a short-lived playback capability after current Location authorization.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Short-lived authorized playback capability.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoicemailPlaybackCapability"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/voicemail-playback/{token}:
    get:
      operationId: getCallingVoicemailPlayback
      summary: Stream Telnyx-owned voicemail audio through a short-lived bearer capability without exposing the provider URL.
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 2048
        - name: Range
          in: header
          required: false
          description: Optional single byte range forwarded to the provider audio request.
          schema:
            type: string
            maxLength: 128
      responses:
        "200":
          description: Authorized complete voicemail audio.
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        "206":
          description: Authorized partial voicemail audio.
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        "403":
          $ref: "#/components/responses/Denied"
        "404":
          description: The Telnyx recording is no longer available.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "503":
          $ref: "#/components/responses/Unavailable"
        "504":
          description: Telnyx recording retrieval timed out.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /v1/calling/calls/{callId}/recording-playback:
    post:
      operationId: issueCallingRecordingPlayback
      summary: Issue a short-lived connected-call recording playback capability after current Location authorization.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Short-lived authorized playback capability.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordingPlaybackCapability"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/calling/recording-playback/{token}:
    get:
      operationId: getCallingRecordingPlayback
      summary: Stream Telnyx-owned connected-call audio through a short-lived bearer capability without exposing the provider URL.
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 2048
        - name: Range
          in: header
          required: false
          description: Optional single byte range forwarded to the provider audio request.
          schema:
            type: string
            maxLength: 128
      responses:
        "200":
          description: Authorized complete connected-call audio.
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        "206":
          description: Authorized partial connected-call audio.
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        "403":
          $ref: "#/components/responses/Denied"
        "404":
          description: The Telnyx recording is no longer available.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "503":
          $ref: "#/components/responses/Unavailable"
        "504":
          description: Telnyx recording retrieval timed out.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /v1/engagements/query:
    post:
      operationId: queryEngagements
      summary: Resolve one authorized exact-phone Engagement across Practice Locations.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EngagementQueryRequest"
      responses:
        "200":
          description: Exact-phone results within the current User's Location Scope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EngagementPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/engagements/{phone}/timeline:
    get:
      operationId: getEngagementTimeline
      summary: Read one authorized exact-phone Engagement History.
      parameters:
        - name: phone
          in: path
          required: true
          schema:
            type: string
            minLength: 8
            maxLength: 32
        - name: practiceId
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
      responses:
        "200":
          description: Chronological exact-phone Engagement History.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationTimelinePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/ai/interactions:
    post:
      operationId: ingestAIInteraction
      summary: Persist one authenticated Abita AI call lifecycle message.
      security:
        - serviceBearer: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AIInteractionIngestRequest"
      responses:
        "200":
          description: Existing Interaction advanced without losing richer evidence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIInteractionReceipt"
        "201":
          description: New Interaction persisted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIInteractionReceipt"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/ai/interactions/{interactionId}:
    get:
      operationId: getAIInteraction
      summary: Read one authorized AI Interaction appointment and call detail.
      parameters:
        - name: interactionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Authorized transcript-free AI Interaction detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIInteractionDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/ai/interactions/{interactionId}/evidence:
    get:
      operationId: getAIInteractionEvidence
      summary: Read one admin-authorized AI Interaction transcript and closeout payload.
      parameters:
        - name: interactionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Admin-authorized AI Interaction evidence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIInteractionEvidence"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/ai/interactions/{interactionId}/review:
    post:
      operationId: reviewAIInteractionOutcome
      summary: Clear the real User's attention marker for one AI appointment outcome.
      parameters:
        - name: interactionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: AI appointment outcome reviewed.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/ai/interactions/outcomes/query:
    post:
      operationId: queryAIInteractionOutcomes
      summary: Read the real User's unreviewed AI appointment outcomes.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AIOutcomeQueryRequest"
      responses:
        "200":
          description: Authorized per-User AI outcome attention projection.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIOutcomePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/operator/ai-analytics/query:
    post:
      operationId: queryOperatorAIAnalytics
      summary: Query bounded AI Interaction analytics as a Platform Operator.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OperatorAIAnalyticsQueryRequest"
      responses:
        "200":
          description: Scoped AI Interaction summary and cursor-paginated calls.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorAIAnalyticsPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/operator/ai-interactions/{interactionId}/analytics:
    get:
      operationId: getOperatorAIInteractionAnalytics
      summary: Read normalized AI Interaction analytics as a Platform Operator.
      parameters:
        - name: interactionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Operator-authorized normalized call detail and evidence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorAIInteractionAnalytics"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/tasks:
    post:
      operationId: createStaffTask
      summary: Accept one authenticated current Abita create_staff_task command.
      security:
        - serviceBearer: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateStaffTaskRequest"
      responses:
        "200":
          description: Safe replay of an already committed Task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffTaskReceipt"
        "201":
          description: Task and source committed atomically.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffTaskReceipt"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/tasks/query:
    post:
      operationId: queryTasks
      summary: Query the authorized Task rail with protected search input.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskQueryRequest"
      responses:
        "200":
          description: Open-first Task rail page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/tasks/{taskId}:
    get:
      operationId: readTask
      summary: Read one currently authorized Task.
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Current Task state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/tasks/{taskId}/title:
    put:
      operationId: renameTask
      summary: Rename one open Task at its last observed version.
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RenameTaskRequest"
      responses:
        "200":
          description: Renamed Task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/tasks/{taskId}/complete:
    post:
      operationId: completeTask
      summary: Complete one Task at its last observed version.
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskTransitionRequest"
      responses:
        "200":
          description: Completed or idempotently current Task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/tasks/{taskId}/reopen:
    post:
      operationId: reopenTask
      summary: Reopen one completed Task at its last observed version.
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaskTransitionRequest"
      responses:
        "200":
          description: Reopened or idempotently current Task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/tasks/{taskId}/history:
    get:
      operationId: getTaskCallHistory
      summary: Read authorized exact-phone Call Engagement History for one Task.
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Newest page presented in chronological order.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallHistoryPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/tasks/{taskId}/engagement-history:
    get:
      operationId: getTaskEngagementHistory
      summary: Read authorized exact-phone Engagement History for one Task.
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
      responses:
        "200":
          description: Chronological exact-phone Engagement History.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationTimelinePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/message-threads/query:
    post:
      operationId: queryMessageThreads
      summary: Query phone-led Message Threads in one authorized Location.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageThreadQueryRequest"
      responses:
        "200":
          description: Newest authorized Message Threads.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageThreadPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/message-threads/{threadId}/timeline:
    get:
      operationId: getMessageThreadTimeline
      summary: Read one authorized living conversation timeline.
      parameters:
        - name: threadId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
      responses:
        "200":
          description: Oldest-to-newest authorized conversation page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationTimelinePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/message-threads/{threadId}/read:
    post:
      operationId: markMessageThreadRead
      summary: Clear the real User's unread marker for one Thread.
      parameters:
        - name: threadId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkMessageThreadReadRequest"
      responses:
        "204":
          description: The User's Thread marker is clear.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/messages:
    post:
      operationId: sendMessage
      summary: Durably accept one location-derived outbound Message.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageRequest"
      responses:
        "200":
          description: Idempotent replay of an existing Message.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageReceipt"
        "201":
          description: New durable Sending Message and provider command.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageReceipt"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/attachments:
    post:
      operationId: uploadMessageAttachment
      summary: Store one private Location-scoped pending Message attachment.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UploadMessageAttachmentRequest"
      responses:
        "201":
          description: Private pending attachment ready for one-time consumption.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageAttachment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/attachments/{attachmentId}:
    get:
      operationId: getMessageAttachment
      summary: Read one stored attachment after current Location authorization.
      parameters:
        - name: attachmentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Authorized attachment bytes with safe response headers.
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/attachments/{attachmentId}/retry-copy:
    post:
      operationId: retryInboundMessageAttachment
      summary: Retry storage of one unavailable inbound attachment in place.
      parameters:
        - name: attachmentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RetryMessageAttachmentRequest"
      responses:
        "200":
          description: The same attachment is Processing again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageAttachment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/messages/{messageId}/send-again:
    post:
      operationId: sendMessageAgain
      summary: Create an explicit new attempt while preserving the original Message.
      parameters:
        - name: messageId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageAgainRequest"
      responses:
        "200":
          description: Idempotent replay of the explicit new attempt.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageReceipt"
        "201":
          description: New immutable Message attempt.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageReceipt"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/messages/{messageId}/follow-up-task:
    post:
      operationId: createMessageFollowUpTask
      summary: Create at most one OPEN follow-up Task from one Message.
      parameters:
        - name: messageId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMessageFollowUpTaskRequest"
      responses:
        "200":
          description: Existing Task returned for a replayed source Message.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "201":
          description: New Message-derived Task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/provider/telnyx/messaging-webhooks:
    post:
      operationId: receiveTelnyxMessagingWebhook
      summary: Verify and durably receipt an inbound Telnyx Message event.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "204":
          description: Receipt durably committed or accepted duplicate.
        "400":
          $ref: "#/components/responses/BadRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/provider/telnyx/messaging-webhooks/{callbackToken}:
    post:
      operationId: receiveCorrelatedTelnyxMessagingWebhook
      summary: Verify and receipt callback-correlated outbound Message evidence.
      security: []
      parameters:
        - name: callbackToken
          in: path
          required: true
          schema:
            type: string
            minLength: 40
            maxLength: 100
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "204":
          description: Receipt durably committed or accepted duplicate.
        "400":
          $ref: "#/components/responses/BadRequest"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/provider/messaging-media/{attachmentId}:
    get:
      operationId: getProviderMessageMedia
      summary: Serve one short-lived signed outbound attachment to Telnyx.
      security: []
      parameters:
        - name: attachmentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: expires
          in: query
          required: true
          schema:
            type: string
        - name: signature
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Intended attachment bytes.
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "403":
          $ref: "#/components/responses/Denied"
  /v1/operator/calls/{callId}/timeline:
    get:
      operationId: getOperatorCallingTimeline
      summary: Read one sanitized durable Call timeline as a Platform Operator.
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Sanitized provider and workflow history.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorCallingTimeline"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "503":
          $ref: "#/components/responses/Unavailable"
  /v1/operator/practices/{practiceId}/provider-receipts/{receiptReference}/requeue:
    post:
      operationId: requeueOperatorProviderReceipt
      summary: Requeue one quarantined provider receipt as a Platform Operator.
      parameters:
        - name: practiceId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: receiptReference
          in: path
          required: true
          schema:
            type: string
            pattern: '^[A-Za-z0-9_-]{43}$'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderReceiptRecoveryRequest"
      responses:
        "200":
          description: The persisted receipt was authorized, audited, and requeued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderReceiptRecovery"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Denied"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/Unavailable"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    serviceBearer:
      type: http
      scheme: bearer
      bearerFormat: opaque-service-token
  responses:
    BadRequest:
      description: Invalid request.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Unauthenticated:
      description: Missing or invalid credential.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Denied:
      description: Current identity lacks the requested authority.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Conflict:
      description: The requested transition is no longer available.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Unavailable:
      description: A required dependency is temporarily unavailable.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
  schemas:
    Health:
      type: object
      additionalProperties: false
      required: [status, role]
      properties:
        status:
          type: string
          enum: [ok]
        role:
          type: string
          enum: [portal-api, provider-ingress, realtime]
    ErrorEnvelope:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          type: object
          additionalProperties: false
          required: [code, message, correlationId, retryable]
          properties:
            code:
              type: string
            message:
              type: string
            correlationId:
              type: string
            retryable:
              type: boolean
    SignUpEligibilityRequest:
      type: object
      additionalProperties: false
      required: [email]
      properties:
        email:
          type: string
          format: email
          maxLength: 320
    SignUpEligibility:
      type: object
      additionalProperties: false
      required: [kind, email]
      properties:
        kind:
          type: string
          enum: [ACCESS_GRANT, PLATFORM_OPERATOR]
        email:
          type: string
          format: email
    Actor:
      type: object
      additionalProperties: false
      required: [subject, email, type]
      properties:
        subject:
          type: string
        email:
          type: string
          format: email
        type:
          type: string
          enum: [HUMAN, PLATFORM_OPERATOR]
    Practice:
      type: object
      additionalProperties: false
      required: [id, name, version]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        version:
          type: integer
          format: int64
          minimum: 1
    Location:
      type: object
      additionalProperties: false
      required: [id, name]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
    Membership:
      type: object
      additionalProperties: false
      required: [id, role, locationScope]
      properties:
        id:
          type: string
          format: uuid
        role:
          type: string
          enum: [ADMIN, STAFF]
        locationScope:
          type: string
          enum: [ALL, SELECTED]
    PracticeAccess:
      allOf:
        - $ref: "#/components/schemas/Practice"
        - type: object
          additionalProperties: false
          required: [locations, callingEnabled]
          properties:
            membership:
              $ref: "#/components/schemas/Membership"
            locations:
              type: array
              items:
                $ref: "#/components/schemas/Location"
            callingEnabled:
              type: boolean
    AccessDiscovery:
      type: object
      additionalProperties: false
      required: [actor, platformOperator, practices]
      properties:
        actor:
          $ref: "#/components/schemas/Actor"
        platformOperator:
          type: boolean
        practices:
          type: array
          items:
            $ref: "#/components/schemas/PracticeAccess"
    WorkspaceSnapshot:
      type: object
      additionalProperties: false
      required:
        - schemaVersion
        - version
        - state
        - actor
        - practice
        - location
        - navigation
        - platformOperator
      properties:
        schemaVersion:
          type: string
          enum: ["2026-07-24"]
        version:
          type: integer
          format: int64
          minimum: 1
        state:
          type: string
          enum: [EMPTY]
        actor:
          $ref: "#/components/schemas/Actor"
        practice:
          $ref: "#/components/schemas/Practice"
        location:
          $ref: "#/components/schemas/Location"
        membership:
          $ref: "#/components/schemas/Membership"
        platformOperator:
          type: boolean
        navigation:
          type: array
          items:
            $ref: "#/components/schemas/NavigationItem"
    NavigationItem:
      type: object
      additionalProperties: false
      required: [id, label, enabled]
      properties:
        id:
          type: string
          enum: [tasks, messages, call-center, recordings, settings]
        label:
          type: string
        enabled:
          type: boolean
    ProviderReceiptRecoveryRequest:
      type: object
      additionalProperties: false
    ProviderReceiptRecovery:
      type: object
      additionalProperties: false
      required: [receiptReference, state]
      properties:
        receiptReference:
          type: string
          pattern: '^[A-Za-z0-9_-]{43}$'
        state:
          type: string
          enum: [PENDING]
    AddLocationRequest:
      type: object
      additionalProperties: false
      required: [key, name]
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 200
    AuditEvent:
      type: object
      additionalProperties: false
      required: [id, actorSubject, practiceId, action, createdAt]
      properties:
        id:
          type: string
          format: uuid
        actorSubject:
          type: string
        practiceId:
          type: string
          format: uuid
        action:
          type: string
        reason:
          type: string
        createdAt:
          type: string
          format: date-time
    LocationMutation:
      type: object
      additionalProperties: false
      required: [location, practiceVersion, audit]
      properties:
        location:
          $ref: "#/components/schemas/Location"
        practiceVersion:
          type: integer
          format: int64
        audit:
          $ref: "#/components/schemas/AuditEvent"
    ContactContextInput:
      type: object
      additionalProperties: false
      required: [phone]
      properties:
        phone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        phoneSource:
          type: string
          maxLength: 100
        displayName:
          type: string
          maxLength: 200
        nameSource:
          type: string
          maxLength: 100
        transferReason:
          type: string
          maxLength: 500
        reasonSource:
          type: string
          maxLength: 100
    CreateHandoffRequest:
      oneOf:
        - type: object
          additionalProperties: false
          required: [practiceId, officeKey, sourceCallId, idempotencyKey, contact]
          properties:
            practiceId:
              type: string
              format: uuid
            officeKey:
              type: string
              pattern: "^[a-z0-9][a-z0-9-]{0,99}$"
              description: Preferred Abita Office Route identity.
            sourceCallId:
              type: string
              minLength: 1
              maxLength: 200
            idempotencyKey:
              type: string
              minLength: 1
              maxLength: 200
            contact:
              $ref: "#/components/schemas/ContactContextInput"
        - type: object
          additionalProperties: false
          required: [practiceId, locationId, sourceCallId, idempotencyKey, contact]
          properties:
            practiceId:
              type: string
              format: uuid
            locationId:
              type: string
              format: uuid
              description: Migration-only compatibility input.
            sourceCallId:
              type: string
              minLength: 1
              maxLength: 200
            idempotencyKey:
              type: string
              minLength: 1
              maxLength: 200
            contact:
              $ref: "#/components/schemas/ContactContextInput"
    Handoff:
      type: object
      additionalProperties: false
      required: [id, sipDestination, expiresAt]
      properties:
        id:
          type: string
          format: uuid
        sipDestination:
          type: string
        expiresAt:
          type: string
          format: date-time
    SoftphoneLeaseRequest:
      type: object
      additionalProperties: false
      required: [sessionId, takeover]
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 200
        takeover:
          type: boolean
    CallingReadinessRequest:
      type: object
      additionalProperties: false
      required:
        - sessionId
        - registered
        - microphoneReady
        - audioReady
        - sessionHealthy
        - available
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 200
        registered:
          type: boolean
        microphoneReady:
          type: boolean
        audioReady:
          type: boolean
        sessionHealthy:
          type: boolean
        available:
          type: boolean
    SoftphoneState:
      type: object
      additionalProperties: false
      required: [sessionId, leaseExpiresAt, owner, available, activeCallId, pendingOutcomeCallId]
      properties:
        sessionId:
          type: string
        leaseExpiresAt:
          type: string
          format: date-time
        owner:
          type: boolean
        available:
          type: boolean
        activeCallId:
          type: string
          description: The User's current durable Call, or empty when capacity is free.
        pendingOutcomeCallId:
          type: string
          description: The latest connected Call awaiting an outcome, or empty when none remains.
    MediaTokenRequest:
      type: object
      additionalProperties: false
      required: [sessionId]
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 200
    MediaToken:
      type: object
      additionalProperties: false
      required: [token, expiresAt]
      properties:
        token:
          type: string
        expiresAt:
          type: string
          format: date-time
    CallingState:
      type: object
      additionalProperties: false
      required: [softphone, ringing]
      properties:
        softphone:
          $ref: "#/components/schemas/SoftphoneState"
        ringing:
          type: array
          items:
            $ref: "#/components/schemas/RingingCallLeg"
        bridged:
          $ref: "#/components/schemas/CallingStateCall"
        voicemail:
          $ref: "#/components/schemas/CallingStateCall"
        disposition:
          $ref: "#/components/schemas/CallingStateCall"
    RingingCallLeg:
      type: object
      additionalProperties: false
      required: [callId, callLegId, mediaToken, practiceId, locationId, locationName, displayName, phone, transferReason, state, version, createdAt, deadline]
      properties:
        callId:
          type: string
          format: uuid
        callLegId:
          type: string
          format: uuid
        mediaToken:
          type: string
          pattern: '^[A-Za-z0-9_-]{43}$'
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        displayName:
          type: string
        phone:
          type: string
          minLength: 1
        transferReason:
          type: string
        state:
          type: string
          enum: [PENDING, DIALING, RINGING, BRIDGE_PENDING]
        version:
          type: integer
          format: int64
          minimum: 1
        createdAt:
          type: string
          format: date-time
        deadline:
          type: string
          format: date-time
    CallingStateCall:
      type: object
      additionalProperties: false
      required: [callId, callLegId, practiceId, locationId, locationName, state, version]
      properties:
        callId:
          type: string
          format: uuid
        callLegId:
          type: string
          format: uuid
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        state:
          type: string
        version:
          type: integer
          format: int64
          minimum: 1
    CallingControlRequest:
      type: object
      additionalProperties: false
      required: [sessionId]
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 200
    StartOutboundCallRequest:
      type: object
      additionalProperties: false
      required: [sessionId, idempotencyKey]
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 200
        idempotencyKey:
          type: string
          pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,199}$'
        taskId:
          type: string
          format: uuid
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        destination:
          type: string
          pattern: '^\+1[2-9][0-9]{2}[2-9][0-9]{6}$'
    RetryOutboundCallRequest:
      type: object
      additionalProperties: false
      required: [sessionId, idempotencyKey]
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 200
        idempotencyKey:
          type: string
          pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,199}$'
    ConfirmCallingMediaRequest:
      type: object
      additionalProperties: false
      required: [sessionId, mediaToken]
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 200
        mediaToken:
          type: string
          minLength: 1
          maxLength: 200
    OutboundCallEligibility:
      type: object
      additionalProperties: false
      required: [eligible, reason]
      properties:
        eligible:
          type: boolean
        reason:
          type: string
    CallingVoicemail:
      type: object
      additionalProperties: false
      required: [outcome, taskId, durationSeconds]
      properties:
        outcome:
          type: string
          enum: [VOICEMAIL, MISSED_CALL]
        audioState:
          type: string
          enum: [PROCESSING, READY, UNAVAILABLE]
        taskId:
          type: string
          format: uuid
        durationSeconds:
          type: integer
          format: int64
          minimum: 0
    CallingRecording:
      type: object
      additionalProperties: false
      required: [audioState, durationSeconds]
      properties:
        audioState:
          type: string
          enum: [PROCESSING, READY, UNAVAILABLE, EXPIRED, DELETED]
        durationSeconds:
          type: integer
          format: int64
          minimum: 0
    VoicemailPlaybackCapability:
      type: object
      additionalProperties: false
      required: [token, expiresAt]
      properties:
        token:
          type: string
        expiresAt:
          type: string
          format: date-time
    RecordingPlaybackCapability:
      type: object
      additionalProperties: false
      required: [token, expiresAt]
      properties:
        token:
          type: string
        expiresAt:
          type: string
          format: date-time
    CallingCall:
      type: object
      additionalProperties: false
      required:
        - id
        - practiceId
        - locationId
        - locationName
        - direction
        - entryPoint
        - state
        - phone
        - phoneSource
        - displayName
        - nameSource
        - transferReason
        - reasonSource
        - providerTermination
        - callerId
        - retryAllowed
        - version
      properties:
        id:
          type: string
          format: uuid
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        direction:
          type: string
          enum: [INBOUND, OUTBOUND]
        entryPoint:
          type: string
          enum: [AI_HANDOFF, TASK, STANDALONE]
        taskId:
          type: string
          format: uuid
        state:
          type: string
          enum:
            - PREPARING
            - RINGING
            - CONNECTING
            - CONNECTED
            - VOICEMAIL_GREETING
            - VOICEMAIL_RECORDING
            - UNANSWERED
            - VOICEMAIL
            - MISSED
            - NEEDS_DISPOSITION
            - RESOLVED
            - FOLLOW_UP_REQUIRED
        dispositionDeadline:
          type: string
          format: date-time
        phone:
          type: string
        phoneSource:
          type: string
        displayName:
          type: string
        nameSource:
          type: string
        transferReason:
          type: string
        reasonSource:
          type: string
        providerTermination:
          type: string
        callerId:
          type: string
        retryOfCallId:
          type: string
          format: uuid
        retryAllowed:
          type: boolean
        connectedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
          minimum: 1
        voicemail:
          $ref: "#/components/schemas/CallingVoicemail"
        recording:
          $ref: "#/components/schemas/CallingRecording"
        recoveryTask:
          $ref: "#/components/schemas/CallingRecoveryTask"
    CallingRecoveryTask:
      type: object
      additionalProperties: false
      required: [id, title, state, relatedInteractionCount]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        state:
          type: string
          enum: [OPEN, COMPLETED]
        relatedInteractionCount:
          type: integer
          minimum: 0
    CallingDispositionRequest:
      type: object
      additionalProperties: false
      required: [sessionId, outcome]
      properties:
        sessionId:
          type: string
          minLength: 1
        outcome:
          type: string
          enum:
            - RESOLVED
            - FOLLOW_UP_REQUIRED
            - COMPLETE_TASK
            - KEEP_OPEN
            - CREATE_TASK
            - NO_FOLLOW_UP
    CallingDispositionResult:
      type: object
      additionalProperties: false
      required: [call]
      properties:
        call:
          $ref: "#/components/schemas/CallingCall"
        taskId:
          type: string
          format: uuid
    StaffTaskCategory:
      type: string
      enum:
        - billing
        - appointments
        - documentation
        - optical
        - medication
        - referrals
        - other
    StaffTaskUrgency:
      type: string
      enum: [high_priority, normal, non_urgent]
    StaffTaskPatientCompatibility:
      type: object
      description: >
        Compatibility-only fields sent by the existing Abita tool. Acuity may
        source-label the optional name but never persists the ID or date of birth.
      additionalProperties: false
      properties:
        id:
          type: string
          maxLength: 255
        name:
          type: string
          maxLength: 200
        dob:
          type: string
          maxLength: 64
    CreateStaffTaskRequest:
      type: object
      additionalProperties: false
      required:
        - callId
        - callerPhone
        - category
        - idempotencyKey
        - message
        - officeKey
        - officePhone
        - source
        - summary
        - urgency
      properties:
        callId:
          type: string
          minLength: 1
          maxLength: 255
        callerPhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        category:
          $ref: "#/components/schemas/StaffTaskCategory"
        idempotencyKey:
          type: string
          minLength: 1
          maxLength: 200
          pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]{0,199}$'
        inboundOfficePhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        message:
          type: string
          minLength: 1
          maxLength: 2500
        officeKey:
          type: string
          minLength: 1
          maxLength: 100
        officePhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        patient:
          $ref: "#/components/schemas/StaffTaskPatientCompatibility"
        source:
          type: string
          enum: [agent]
        summary:
          type: string
          minLength: 1
          maxLength: 240
        urgency:
          $ref: "#/components/schemas/StaffTaskUrgency"
    StaffTaskReceipt:
      type: object
      additionalProperties: false
      required: [status, taskId, category, urgency]
      properties:
        status:
          type: string
          enum: [created, duplicate]
        taskId:
          type: string
          format: uuid
        category:
          $ref: "#/components/schemas/StaffTaskCategory"
        urgency:
          $ref: "#/components/schemas/StaffTaskUrgency"
    TaskActor:
      type: object
      additionalProperties: false
      required: [kind, subject]
      properties:
        kind:
          type: string
          enum: [HUMAN, SERVICE]
        subject:
          type: string
        email:
          type: string
          format: email
    Task:
      type: object
      additionalProperties: false
      required:
        - id
        - practiceId
        - locationId
        - locationName
        - phone
        - title
        - state
        - origin
        - urgency
        - createdBy
        - createdAt
        - unread
        - version
        - updatedAt
        - relatedInteractionCount
        - interactions
      properties:
        id:
          type: string
          format: uuid
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        callId:
          type: string
          format: uuid
        phone:
          type: string
        title:
          type: string
        state:
          type: string
          enum: [OPEN, COMPLETED]
        origin:
          type: string
          enum:
            - HUMAN_CALL_FOLLOW_UP
            - ABITA_AI
            - STAFF_MESSAGE_FOLLOW_UP
            - VOICEMAIL_RECOVERY
            - MISSED_CALL_RECOVERY
        recoveryOutcome:
          type: string
          enum: [VOICEMAIL, MISSED_CALL]
        urgency:
          $ref: "#/components/schemas/StaffTaskUrgency"
        category:
          $ref: "#/components/schemas/StaffTaskCategory"
        callerName:
          type: string
        sourceCallId:
          type: string
        sourceMessage:
          type: string
        messageId:
          type: string
          format: uuid
        messageThreadId:
          type: string
          format: uuid
        conversationThreadId:
          type: string
          format: uuid
        unread:
          type: boolean
        createdBy:
          $ref: "#/components/schemas/TaskActor"
        createdAt:
          type: string
          format: date-time
        completedBy:
          $ref: "#/components/schemas/TaskActor"
        completedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
          minimum: 1
        updatedAt:
          type: string
          format: date-time
        relatedInteractionCount:
          type: integer
          minimum: 0
        interactions:
          type: array
          items:
            $ref: "#/components/schemas/TaskInteraction"
    TaskInteraction:
      type: object
      additionalProperties: false
      required: [callId, occurredAt, type]
      properties:
        callId:
          type: string
          format: uuid
        occurredAt:
          type: string
          format: date-time
        type:
          type: string
          enum: [CALL, VOICEMAIL]
    TaskPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor, counts]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Task"
        nextCursor:
          type: string
        counts:
          $ref: "#/components/schemas/TaskFolderCounts"
    TaskFolderCounts:
      type: object
      additionalProperties: false
      required: [tasks, missedCalls, categories]
      properties:
        tasks:
          type: integer
          minimum: 0
        missedCalls:
          type: integer
          minimum: 0
        categories:
          $ref: "#/components/schemas/TaskCategoryCounts"
    TaskCategoryCounts:
      type: object
      additionalProperties: false
      required: [billing, appointments, documentation, optical, medication, referrals, other]
      properties:
        billing:
          type: integer
          minimum: 0
        appointments:
          type: integer
          minimum: 0
        documentation:
          type: integer
          minimum: 0
        optical:
          type: integer
          minimum: 0
        medication:
          type: integer
          minimum: 0
        referrals:
          type: integer
          minimum: 0
        other:
          type: integer
          minimum: 0
    EngagementLocation:
      type: object
      additionalProperties: false
      required: [id, name]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
    EngagementSummary:
      type: object
      additionalProperties: false
      required:
        - phone
        - locations
        - latestActivity
        - openTaskCount
        - unread
      properties:
        phone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        displayName:
          type: string
        locations:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/EngagementLocation"
        latestActivity:
          type: string
          format: date-time
        openTaskCount:
          type: integer
          minimum: 0
        unread:
          type: boolean
    EngagementPage:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/EngagementSummary"
    EngagementQueryRequest:
      type: object
      additionalProperties: false
      required: [practiceId, phone]
      properties:
        practiceId:
          type: string
          format: uuid
        phone:
          type: string
          minLength: 1
          maxLength: 32
    AIInteractionMessageKind:
      type: string
      enum: [START, SUMMARY, CLOSEOUT, OUTCOME_CHECKPOINT]
    AIInteractionCallStatus:
      type: string
      enum: [IN_PROGRESS, COMPLETED, ESCALATED, FAILED]
    AIAppointmentAction:
      type: string
      enum: [BOOKED, CANCELLED, RESCHEDULED]
    AIAppointmentEvidence:
      type: object
      additionalProperties: false
      required: [action, occurredAt]
      properties:
        action:
          $ref: "#/components/schemas/AIAppointmentAction"
        occurredAt:
          type: string
          format: date-time
        externalPatientId:
          type: string
          minLength: 1
          maxLength: 255
        oldAppointmentId:
          type: string
          minLength: 1
          maxLength: 255
        newAppointmentId:
          type: string
          minLength: 1
          maxLength: 255
        bookingResult:
          type: object
          additionalProperties: true
        cancellationResult:
          type: object
          additionalProperties: true
    AIInteractionIngestRequest:
      type: object
      description: >-
        One lifecycle envelope. START uses IN_PROGRESS without lifecycle evidence;
        OUTCOME_CHECKPOINT uses IN_PROGRESS with appointmentOutcome; SUMMARY uses a
        terminal status with endedAt and summaryPayload; CLOSEOUT uses a terminal
        status with endedAt and closeoutPayload. The service validates these
        kind-specific requirements.
      additionalProperties: false
      required:
        - kind
        - sourceCallId
        - callerPhone
        - officePhone
        - startedAt
        - status
      properties:
        kind:
          $ref: "#/components/schemas/AIInteractionMessageKind"
        officeKey:
          type: string
          minLength: 1
          maxLength: 100
        sourceCallId:
          type: string
          minLength: 1
          maxLength: 255
        callerPhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        officePhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        startedAt:
          type: string
          format: date-time
        endedAt:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/AIInteractionCallStatus"
        summary:
          type: string
          minLength: 1
          maxLength: 10000
        transcript:
          type: object
          additionalProperties: true
        appointmentOutcome:
          $ref: "#/components/schemas/AIAppointmentEvidence"
        summaryPayload:
          type: object
          additionalProperties: true
        closeoutPayload:
          type: object
          additionalProperties: true
    AIInteractionReceipt:
      type: object
      additionalProperties: false
      required: [interactionId, status]
      properties:
        interactionId:
          type: string
          format: uuid
        status:
          type: string
          enum: [created, updated]
    AIAppointmentOutcome:
      type: string
      enum: [BOOKING, CANCELLATION, RESCHEDULE, PARTIAL, INDETERMINATE]
    AIAppointmentFacts:
      type: object
      additionalProperties: false
      properties:
        appointmentDate:
          type: string
        appointmentId:
          type: string
        appointmentTime:
          type: string
        appointmentTypeName:
          type: string
        careLane:
          type: string
        locationName:
          type: string
        patientName:
          type: string
        providerName:
          type: string
        startDatetime:
          type: string
    AIInteractionDetail:
      type: object
      additionalProperties: false
      required:
        - id
        - practiceId
        - locationId
        - locationName
        - sourceCallId
        - phone
        - officePhone
        - startedAt
        - status
        - appointmentOutcome
        - appointment
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          format: uuid
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        sourceCallId:
          type: string
        phone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        officePhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        externalPatientId:
          type: string
        startedAt:
          type: string
          format: date-time
        endedAt:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/AIInteractionCallStatus"
        summary:
          type: string
        appointmentAction:
          $ref: "#/components/schemas/AIAppointmentAction"
        appointmentOutcome:
          $ref: "#/components/schemas/AIAppointmentOutcome"
        appointment:
          $ref: "#/components/schemas/AIAppointmentFacts"
        previousAppointment:
          $ref: "#/components/schemas/AIAppointmentFacts"
        appointmentOccurredAt:
          type: string
          format: date-time
        oldAppointmentId:
          type: string
        newAppointmentId:
          type: string
        bookingResult:
          type: object
          additionalProperties: true
        cancellationResult:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    AIInteractionEvidence:
      type: object
      additionalProperties: false
      required: [id, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        transcript:
          type: object
          additionalProperties: true
        closeoutPayload:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    AIOutcomeQueryRequest:
      type: object
      additionalProperties: false
      required: [practiceId]
      properties:
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        appointmentAction:
          $ref: "#/components/schemas/AIAppointmentAction"
        includeCounts:
          type: boolean
          default: true
        cursor:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 50
    AIOutcomeItem:
      type: object
      additionalProperties: false
      required:
        - id
        - locationId
        - locationName
        - sourceCallId
        - phone
        - startedAt
        - status
        - appointmentOutcome
      properties:
        id:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        sourceCallId:
          type: string
        phone:
          type: string
        externalPatientId:
          type: string
        startedAt:
          type: string
          format: date-time
        endedAt:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/AIInteractionCallStatus"
        summary:
          type: string
        appointmentAction:
          $ref: "#/components/schemas/AIAppointmentAction"
        appointmentOutcome:
          $ref: "#/components/schemas/AIAppointmentOutcome"
        appointmentOccurredAt:
          type: string
          format: date-time
        oldAppointmentId:
          type: string
        newAppointmentId:
          type: string
    AIOutcomePage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AIOutcomeItem"
        nextCursor:
          type: string
        counts:
          $ref: "#/components/schemas/AIOutcomeCounts"
    AIOutcomeCounts:
      type: object
      additionalProperties: false
      required: [tasks, bookings, cancellations, reschedules]
      properties:
        tasks:
          type: integer
          minimum: 0
        bookings:
          type: integer
          minimum: 0
        cancellations:
          type: integer
          minimum: 0
        reschedules:
          type: integer
          minimum: 0
    OperatorAIAnalyticsRange:
      type: string
      enum: [24h, 7d, 30d]
    OperatorAIAnalyticsQueryRequest:
      type: object
      additionalProperties: false
      required: [practiceId, range]
      properties:
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        range:
          $ref: "#/components/schemas/OperatorAIAnalyticsRange"
        cursor:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
    OperatorAIAnalyticsSummary:
      type: object
      additionalProperties: false
      required:
        - totalCalls
        - bookingCount
        - cancellationCount
        - rescheduleCount
        - transferCount
        - transferRate
        - toolCallCount
        - toolErrorCount
        - toolFailureRate
      properties:
        totalCalls:
          type: integer
          minimum: 0
        bookingCount:
          type: integer
          minimum: 0
        cancellationCount:
          type: integer
          minimum: 0
        rescheduleCount:
          type: integer
          minimum: 0
        p50SttMs:
          type: integer
          minimum: 0
        p90SttMs:
          type: integer
          minimum: 0
        p99SttMs:
          type: integer
          minimum: 0
        p50TtftMs:
          type: integer
          minimum: 0
        p90TtftMs:
          type: integer
          minimum: 0
        p99TtftMs:
          type: integer
          minimum: 0
        p50TtsTtfbMs:
          type: integer
          minimum: 0
        p90TtsTtfbMs:
          type: integer
          minimum: 0
        p99TtsTtfbMs:
          type: integer
          minimum: 0
        p50TotalLatencyMs:
          type: integer
          minimum: 0
        p90TotalLatencyMs:
          type: integer
          minimum: 0
        p99TotalLatencyMs:
          type: integer
          minimum: 0
        transferCount:
          type: integer
          minimum: 0
        transferRate:
          type: number
          format: double
          minimum: 0
          maximum: 1
        toolCallCount:
          type: integer
          minimum: 0
        toolErrorCount:
          type: integer
          minimum: 0
        toolFailureRate:
          type: number
          format: double
          minimum: 0
          maximum: 1
    OperatorAICallAnalytics:
      type: object
      additionalProperties: false
      required:
        - id
        - locationId
        - locationName
        - sourceCallId
        - phone
        - startedAt
        - status
        - durationSeconds
        - toolCallCount
        - toolErrorCount
        - toolActions
        - transferred
        - transcriptAvailable
      properties:
        id:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        sourceCallId:
          type: string
        phone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        startedAt:
          type: string
          format: date-time
        endedAt:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/AIInteractionCallStatus"
        durationSeconds:
          type: integer
          minimum: 0
        p50SttMs:
          type: integer
          minimum: 0
        p50TtftMs:
          type: integer
          minimum: 0
        p50TtsTtfbMs:
          type: integer
          minimum: 0
        p50TotalLatencyMs:
          type: integer
          minimum: 0
        toolCallCount:
          type: integer
          minimum: 0
        toolErrorCount:
          type: integer
          minimum: 0
        toolActions:
          type: array
          items:
            type: string
        transferred:
          type: boolean
        transcriptAvailable:
          type: boolean
    OperatorAIAnalyticsPage:
      type: object
      additionalProperties: false
      required: [summary, calls, nextCursor]
      properties:
        summary:
          $ref: "#/components/schemas/OperatorAIAnalyticsSummary"
        calls:
          type: array
          items:
            $ref: "#/components/schemas/OperatorAICallAnalytics"
        nextCursor:
          type: string
    OperatorAITimelineKind:
      type: string
      enum: [CALLER_MESSAGE, AGENT_MESSAGE, TOOL_CALL, TOOL_RESULT]
    OperatorAITimelineItem:
      type: object
      additionalProperties: false
      required: [kind, occurredAt]
      properties:
        kind:
          $ref: "#/components/schemas/OperatorAITimelineKind"
        occurredAt:
          type: string
          format: date-time
        text:
          type: string
        name:
          type: string
        callId:
          type: string
        payload:
          type: object
          additionalProperties: true
        error:
          type: string
        sttMs:
          type: integer
          minimum: 0
        ttftMs:
          type: integer
          minimum: 0
        ttsTtfbMs:
          type: integer
          minimum: 0
        totalLatencyMs:
          type: integer
          minimum: 0
    OperatorAIToolExecution:
      type: object
      additionalProperties: false
      required: [callId, name, occurredAt, status]
      properties:
        callId:
          type: string
        name:
          type: string
        occurredAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [SUCCESS, ERROR]
        outputClass:
          type: string
    OperatorAIInteractionAnalytics:
      type: object
      additionalProperties: false
      required:
        - id
        - practiceId
        - locationId
        - locationName
        - sourceCallId
        - phone
        - officePhone
        - startedAt
        - status
        - appointmentOutcome
        - appointment
        - createdAt
        - updatedAt
        - timeline
        - toolExecutions
      properties:
        id:
          type: string
          format: uuid
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        sourceCallId:
          type: string
        phone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        officePhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        externalPatientId:
          type: string
        startedAt:
          type: string
          format: date-time
        endedAt:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/AIInteractionCallStatus"
        summary:
          type: string
        appointmentOutcome:
          $ref: "#/components/schemas/AIAppointmentOutcome"
        appointment:
          $ref: "#/components/schemas/AIAppointmentFacts"
        previousAppointment:
          $ref: "#/components/schemas/AIAppointmentFacts"
        appointmentOccurredAt:
          type: string
          format: date-time
        oldAppointmentId:
          type: string
        newAppointmentId:
          type: string
        bookingResult:
          type: object
          additionalProperties: true
        cancellationResult:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        p50SttMs:
          type: integer
          minimum: 0
        p50TtftMs:
          type: integer
          minimum: 0
        p50TtsTtfbMs:
          type: integer
          minimum: 0
        p50TotalLatencyMs:
          type: integer
          minimum: 0
        timeline:
          type: array
          items:
            $ref: "#/components/schemas/OperatorAITimelineItem"
        toolExecutions:
          type: array
          items:
            $ref: "#/components/schemas/OperatorAIToolExecution"
    TaskQueryRequest:
      type: object
      additionalProperties: false
      required: [practiceId]
      properties:
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        search:
          type: string
          maxLength: 500
        state:
          type: string
          enum: [OPEN, COMPLETED]
        ordering:
          type: string
          enum: [priority, recent, time]
        folder:
          type: string
          enum: [work, missed_calls]
        cursor:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 50
    RenameTaskRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, title]
      properties:
        expectedVersion:
          type: integer
          format: int64
          minimum: 1
        title:
          type: string
          minLength: 1
          maxLength: 500
    TaskTransitionRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion]
      properties:
        expectedVersion:
          type: integer
          format: int64
          minimum: 1
    MessageDirection:
      type: string
      enum: [INBOUND, OUTBOUND]
    MessageDeliveryState:
      type: string
      enum: [Sending, Sent, Delivered, Failed, Status unknown]
    MessageThread:
      type: object
      additionalProperties: false
      required:
        - id
        - practiceId
        - locationId
        - locationName
        - officePhone
        - externalPhone
        - outboundBlocked
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          format: uuid
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        officePhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        externalPhone:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        displayName:
          type: string
        nameSource:
          type: string
        outboundBlocked:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    MessageThreadSummary:
      allOf:
        - $ref: "#/components/schemas/MessageThread"
        - type: object
          additionalProperties: false
          required:
            - preview
            - latestDirection
            - latestDelivery
            - latestActivity
            - openTaskCount
            - unread
          properties:
            preview:
              type: string
            latestDirection:
              $ref: "#/components/schemas/MessageDirection"
            latestDelivery:
              $ref: "#/components/schemas/MessageDeliveryState"
            latestActivity:
              type: string
              format: date-time
            openTaskCount:
              type: integer
              minimum: 0
            unread:
              type: boolean
    MessageThreadPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/MessageThreadSummary"
        nextCursor:
          type: string
    MessageThreadQueryRequest:
      type: object
      additionalProperties: false
      required: [practiceId]
      properties:
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        search:
          type: string
          maxLength: 40
        cursor:
          type: string
        limit:
          type: integer
          minimum: 1
          maximum: 50
    Message:
      type: object
      additionalProperties: false
      required:
        - id
        - thread
        - direction
        - body
        - sender
        - destination
        - delivery
        - createdAt
        - updatedAt
        - version
      properties:
        id:
          type: string
          format: uuid
        thread:
          $ref: "#/components/schemas/MessageThread"
        direction:
          $ref: "#/components/schemas/MessageDirection"
        body:
          type: string
        sender:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        destination:
          type: string
          pattern: '^\+[1-9][0-9]{7,14}$'
        delivery:
          $ref: "#/components/schemas/MessageDeliveryState"
        safeFailureCode:
          type: string
        providerMessageId:
          type: string
        taskId:
          type: string
          format: uuid
        retryOfMessageId:
          type: string
          format: uuid
        attachment:
          $ref: "#/components/schemas/MessageAttachment"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
          minimum: 1
    MessageReceipt:
      type: object
      additionalProperties: false
      required: [status, message]
      properties:
        status:
          type: string
          enum: [created, duplicate]
        message:
          $ref: "#/components/schemas/Message"
    SendMessageRequest:
      type: object
      additionalProperties: false
      required: [practiceId, locationId, body, idempotencyKey]
      properties:
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        threadId:
          type: string
          format: uuid
        destination:
          type: string
          maxLength: 40
        body:
          type: string
          maxLength: 1600
        attachmentId:
          type: string
          format: uuid
        taskId:
          type: string
          format: uuid
        idempotencyKey:
          type: string
          minLength: 1
          maxLength: 200
    MessageAttachment:
      type: object
      additionalProperties: false
      required:
        - id
        - direction
        - state
        - fileName
        - contentType
        - byteSize
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          format: uuid
        messageId:
          type: string
          format: uuid
        direction:
          $ref: "#/components/schemas/MessageDirection"
        state:
          type: string
          enum: [Pending, Processing, Stored, Attachment unavailable]
        fileName:
          type: string
        contentType:
          type: string
          enum:
            - image/jpeg
            - image/png
            - image/gif
            - image/webp
            - application/pdf
        byteSize:
          type: integer
          minimum: 0
          maximum: 614400
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    UploadMessageAttachmentRequest:
      type: object
      additionalProperties: false
      required:
        - practiceId
        - locationId
        - fileName
        - contentType
        - contentBase64
      properties:
        practiceId:
          type: string
          format: uuid
        locationId:
          type: string
          format: uuid
        fileName:
          type: string
          minLength: 1
          maxLength: 255
        contentType:
          type: string
          enum:
            - image/jpeg
            - image/png
            - image/gif
            - image/webp
            - application/pdf
        contentBase64:
          type: string
          minLength: 1
          maxLength: 819200
    RetryMessageAttachmentRequest:
      type: object
      additionalProperties: false
    SendMessageAgainRequest:
      type: object
      additionalProperties: false
      required: [idempotencyKey, duplicateRiskAcknowledged]
      properties:
        idempotencyKey:
          type: string
          minLength: 1
          maxLength: 200
        duplicateRiskAcknowledged:
          type: boolean
    MarkMessageThreadReadRequest:
      type: object
      additionalProperties: false
    CreateMessageFollowUpTaskRequest:
      type: object
      additionalProperties: false
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 500
    ConversationTimelineItem:
      type: object
      additionalProperties: false
      required: [type, id, occurredAt]
      properties:
        type:
          type: string
          enum: [MESSAGE, CALL, AI_INTERACTION, TASK]
        id:
          type: string
          format: uuid
        occurredAt:
          type: string
          format: date-time
        taskActivity:
          type: string
          enum:
            - TASK_CREATED
            - TITLE_CHANGED
            - TASK_COMPLETED
            - TASK_REOPENED
            - INTERACTION_ATTACHED
            - TASK_AUTO_COMPLETED_INBOUND_CALL
            - TASK_AUTO_COMPLETED_BOOKING
            - TASK_AUTO_COMPLETED_DUPLICATE
        message:
          $ref: "#/components/schemas/Message"
        task:
          $ref: "#/components/schemas/Task"
        call:
          $ref: "#/components/schemas/CallHistoryItem"
        aiInteraction:
          $ref: "#/components/schemas/AIOutcomeItem"
    ConversationTimelinePage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ConversationTimelineItem"
        nextCursor:
          type: string
    CallHistoryItem:
      type: object
      additionalProperties: false
      required:
        - id
        - type
        - direction
        - startedAt
        - durationSeconds
        - locationId
        - locationName
        - answeredByEmail
        - transferReason
        - outcome
        - current
        - originating
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          enum: [CALL]
        direction:
          type: string
          enum: [INBOUND, OUTBOUND]
        startedAt:
          type: string
          format: date-time
        endedAt:
          type: string
          format: date-time
        durationSeconds:
          type: integer
          format: int64
          minimum: 0
        locationId:
          type: string
          format: uuid
        locationName:
          type: string
        answeredByEmail:
          type: string
        transferReason:
          type: string
        sourceCallId:
          type: string
        outcome:
          type: string
          enum:
            - PREPARING
            - RINGING
            - CONNECTING
            - CONNECTED
            - UNANSWERED
            - VOICEMAIL
            - MISSED
            - NEEDS_DISPOSITION
            - RESOLVED
            - FOLLOW_UP_REQUIRED
        current:
          type: boolean
        originating:
          type: boolean
    CallHistoryPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/CallHistoryItem"
        nextCursor:
          type: string
    OperatorCallingTimelineEntry:
      type: object
      additionalProperties: false
      required:
        - kind
        - opaqueReference
        - errorCode
        - commandAction
        - commandState
        - commandAttempts
        - receiptState
        - ageSeconds
        - occurredAt
      properties:
        kind:
          type: string
        opaqueReference:
          type: string
        recoveryReference:
          type: string
          pattern: '^[A-Za-z0-9_-]{43}$'
        errorCode:
          type: string
        commandAction:
          type: string
        commandState:
          type: string
        commandAttempts:
          type: integer
          minimum: 0
        receiptState:
          type: string
        ageSeconds:
          type: integer
          format: int64
          minimum: 0
        occurredAt:
          type: string
          format: date-time
    OperatorCallingTimeline:
      type: object
      additionalProperties: false
      required: [callId, practiceId, state, version, entries]
      properties:
        callId:
          type: string
          format: uuid
        practiceId:
          type: string
          format: uuid
        state:
          type: string
          enum:
            - PREPARING
            - RINGING
            - CONNECTING
            - CONNECTED
            - UNANSWERED
            - VOICEMAIL
            - MISSED
            - NEEDS_DISPOSITION
            - RESOLVED
            - FOLLOW_UP_REQUIRED
        version:
          type: integer
          format: int64
          minimum: 1
        entries:
          type: array
          items:
            $ref: "#/components/schemas/OperatorCallingTimelineEntry"
security:
  - bearerAuth: []
